{% extends 'base.html.twig' %}

{% block title %}Evenementiels{% endblock %}

{% block body %}
    <h1>Evenementiels</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ evenementiel.id }}</td>
            </tr>
            <tr>
                <th>Date_evenementiel</th>
                <td>{{ evenementiel.dateEvenementiel ? evenementiel.dateEvenementiel|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('evenementiels_index') }}">back to list</a>

    <a href="{{ path('evenementiels_edit', {'id': evenementiel.id}) }}">edit</a>

    {{ include('evenementiels/_delete_form.html.twig') }}
{% endblock %}
