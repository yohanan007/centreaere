{% extends 'base.html.twig' %}

{% block title %}ActivitesEnfants{% endblock %}

{% block body %}
    <h1>ActivitesEnfants</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ activites_enfant.id }}</td>
            </tr>
            <tr>
                <th>Valid</th>
                <td>{{ activites_enfant.valid ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('activites_enfants_index') }}">back to list</a>

    <a href="{{ path('activites_enfants_edit', {'id': activites_enfant.id}) }}">edit</a>

    {{ include('activites_enfants/_delete_form.html.twig') }}
{% endblock %}
