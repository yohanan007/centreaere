<form method="post" action="{{ path('activites_enfants_delete', {'id': activites_enfant.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ activites_enfant.id) }}">
    <button class="btn">Delete</button>
</form>
